---
description: A fixture deliberately missing the `name:` field so the validator must emit E000_NAME_MISSING.
tier: org
---

The body content is irrelevant — the frontmatter is the breakage point. Do not add a `name:` field here.
