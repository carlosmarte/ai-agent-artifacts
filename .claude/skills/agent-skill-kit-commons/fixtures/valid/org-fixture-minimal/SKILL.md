---
name: org-fixture-minimal
description: A minimal valid fixture skill used to smoke-test the validator and parity harness across both runtimes.
tier: org
---

This is a minimal-but-valid SKILL.md used as a smoke fixture. It carries the spec-required frontmatter
(`name`, `description`, `tier`) and a non-empty body paragraph. Adding or removing fields in this file
will break every parity test downstream.
