---
name: org-parsed-minimal
description: Minimal parsed fixture exercising only the three required frontmatter fields, used to freeze parser-output snapshots for cross-runtime drift detection.
tier: org
---

Body paragraph one. This skill is a parser smoke fixture; the canonical-JSON snapshot
under fixtures/parsed/expected/ is the load-bearing artifact, not the body content.

Body paragraph two — kept short so `body_lines` stays stable.
